# 功能记录

这个文件从 2026-06-09 开始记录客服排班器后续新增功能、规则变更和验证方式。每次增加功能或调整规则时，先写清楚目标、改动范围、验证方式和后续注意点，方便之后继续接手。

## 2026-06-09

### 合并客服排班信息和企微导出信息

- 背景：用户发现 `客服 1-4` 的企微导出信息和 `员工 1-4` 的排班角色信息分成两块维护，容易重复、混乱。
- 改动：
  - 将每个客服合并为一张卡片。
  - 客服卡片内同时维护 `姓名 / 账号 / 部门 / 职务 / 当前角色`。
  - 移除下方重复的 `客服 1-4` 企微导出卡片。
  - 产品运维专员仍单独保留，因为它不参与客服轮休，只参与导出。
  - `下一个休周末的人` 改为读取合并后的客服姓名。
  - 详细排班表、复制、CSV、Excel、企微模板导出继续使用同一份客服信息。
  - 兼容旧本地缓存：旧的员工角色和企微导出信息会自动合并回新卡片。
- 验证：
  - `node tools/check-html.js` 通过。
  - 本地页面验证只有 4 个客服合并卡片，产品运维单独显示。
  - 本地页面验证 `下一个休周末的人` 和详细排班表头读取合并后的客服姓名。

### 排班范围和企微导出范围分离

- 背景：用户指出自然月导出不能截断排班计算范围。例如实际需要从 `2026-05-11` 排到 `2026-06-30`，这样才能知道哪一周是谁开始休周末；但企业微信模板只需要导出 `2026-06-01` 到 `2026-06-30`。
- 改动：
  - 移除上一版的 `排班月份` 和 `套用整月`。
  - 配置区改为两组日期：
    - `排班开始日期 / 排班结束日期`：用于完整计算轮休和详细排班。
    - `企微导出开始日期 / 企微导出结束日期`：只控制企业微信模板导出的日期列。
  - 默认排班范围改为 `2026-05-11` 到 `2026-06-30`。
  - 默认企微导出范围改为 `2026-06-01` 到 `2026-06-30`。
  - 企微导出会先从完整排班中筛选导出日期，再生成模板。
  - 导出范围必须落在排班范围内，避免导出缺少轮休上下文的日期。
- 验证：
  - `node tools/check-html.js` 通过。
  - 本地页面验证完整排班范围 `2026-05-11` 到 `2026-06-30`，共 51 天。
  - 本地页面验证企微导出范围 `2026-06-01` 到 `2026-06-30`，共 30 天。
  - 验证旧的 `排班月份` 控件已移除。

### 按自然月和日期范围排班

- 背景：用户指出实际通常是排一个月的班，不是固定几周；而且每个月 1 号不一定是周一，只有开始日期会让自然月排班不顺手。
- 改动：
  - 配置区新增 `排班月份`、`结束日期` 和 `套用整月`。
  - 默认范围改为 `2026-06-01` 到 `2026-06-30`。
  - 生成逻辑从“开始日期 + 周数”改为“开始日期 + 结束日期”。
  - 周轮换仍按日历周计算，日期范围只决定导出哪些天。
  - 每天按真实星期几套班型，解决非周一开始时第一天被当成周一的问题。
  - 旧版本地缓存里如果只有 `weekCount`，会自动换算成结束日期，避免已有配置直接失效。
- 验证：
  - `node tools/check-html.js` 通过。
  - 本地页面验证 2026 年 5 月整月会生成 `2026-05-01` 到 `2026-05-31`，共 31 天。
  - 验证 2026-05-01 第一行显示 `周五`，2026-05-31 最后一行显示 `周日`。

### 项目化并上传 GitHub

- 背景：用户希望排班器像 `redpoint` 项目一样上传到 GitHub，并且每次更新都有记录，方便持续维护进度。
- 改动：
  - 将当前排班器整理为 GitHub 静态站点，入口为 `index.html`。
  - 新增 `README.md`、`FEATURE_LOG.md`、`agent.md`。
  - 新增 `docs/CHANGELOG.md`、`docs/PROJECT_STATUS.md`、`docs/HANDOFF.md`。
  - 新增 `tools/sync-start.ps1` 与 `tools/sync-finish.ps1`，固定开始同步与收尾提交流程。
  - 新增 `tools/check-html.js`，用于检查 `index.html` 内嵌脚本语法。
- 验证：
  - `node tools/check-html.js` 通过。
  - GitHub 仓库已创建并推送：`https://github.com/Lucifer7012/customer-service-scheduler`。
  - GitHub Pages 已启用：`https://lucifer7012.github.io/customer-service-scheduler/`。

### 当前排班器功能基线

- 背景：在上传前固定当前已完成的可用功能，避免后续接力时丢失上下文。
- 已包含能力：
  - 四名海外游戏客服按角色排班。
  - 每四周轮换休周末人员，并支持手动指定下一位休周末的人。
  - 产品运维专员独立固定排班：`白班 / 晚班 / 白班 / 晚班 / 休息 / 休息 / 晚班`。
  - 排班器页面展示企微导出信息，可编辑姓名、账号、部门、职务。
  - 职务选项包含 `选填`、`产品运维专员`、`海外游戏客服`。
  - 企业微信模板导出包含顶部标题、班次说明、日期、星期、产品运维与客服排班。
- 后续注意：
  - 企业微信导出内的休息班次必须写作 `休息`，不要改回 `休假`。
  - 第四位客服暂未到岗时继续使用 `客服D` 作为占位。
